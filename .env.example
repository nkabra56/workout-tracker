# Copy to a private server-side environment file; never commit real values.
HOST=127.0.0.1
PORT=5173
DATA_DIR=./private-data
# Minimum 32 characters. Empty disables sync (503), never opens anonymous access.
SYNC_TOKEN=
# Required in private HTTPS deployment. Exact browser origin, no trailing slash.
APP_ORIGIN=
